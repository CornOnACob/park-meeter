import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  amount: {
    textAlign: "center",
    fontSize: 30,
    fontWeight: "bold",
    paddingTop: 90,
    paddingBottom: 50,
  },
})
